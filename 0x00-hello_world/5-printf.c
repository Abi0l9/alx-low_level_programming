#include <stdio.h>
/**
 * main - this function prints a string.
 * Return: return 0 if the code runs successfully,
 * 1, otherwise.
 */
int main(void)
{
	printf("%s", "with proper grammar, but the outcome is a piece of art,\n");

	return (0);
}
